# AGENTS.md

이 파일은 이 저장소에서 Codex가 작업할 때 따르는 프로젝트 전용 운영 규칙이다.

## 기본 원칙

- 답변과 작업 기록은 한국어로 작성한다.
- 변경 전에는 `package.json`, 기존 문서, 현재 git 상태를 먼저 확인한다.
- 추정 대신 로컬 파일과 명령 실행 결과를 우선한다.
- 작업으로 코드나 설정이 바뀌면 관련 `docs/` 문서도 함께 갱신한다.
- 사용자 변경은 되돌리지 않는다.
- 작업 종료 전 관련 테스트를 실행하고 결과를 `progress.md`에 남긴다.
- 사용자가 중단을 요청하지 않았다면 변경 사항을 commit/push까지 진행한다.

## 세션 시작 프로토콜

1. `pwd`로 작업 디렉토리를 확인한다.
2. `git log -5 --oneline`으로 최근 변경 이력을 확인한다.
3. `progress.md`를 읽어 현재 상태를 파악한다.
4. `feature-list.json`에서 `passes: false`인 최우선 항목을 하나 선택한다.
5. `./init.sh`로 기본 검증과 개발 서버 시작을 수행한다.
6. 선택한 항목 하나만 구현한다.
7. 관련 lint/build/test를 실행한다.
8. 검증 결과를 `progress.md`에 업데이트한다.
9. 필요한 경우 `feature-list.json`의 해당 항목 `passes`만 갱신한다.
10. git commit 후 push한다.

## SSOT 파일

| 파일 | 역할 | 관리 원칙 |
|------|------|-----------|
| `AGENTS.md` | Codex 작업 규칙과 레포 운영 기준 | 프로젝트 운영 규칙 변경 시 갱신 |
| `README.md` | 사용자/운영자용 프로젝트 개요와 워크플로우 | 공개 문서 기준 |
| `CLAUDE.md` | Claude Code용 코드베이스 가이드 | 아키텍처와 명령어 변경 시 갱신 |
| `feature-list.json` | 구현/정리할 작업 목록 | AI는 기존 항목의 `passes`만 변경 가능 |
| `progress.md` | 세션 진행 상태와 검증 결과 | 매 세션 종료 시 갱신 |
| Git 로그 | 변경 이력 | 의미 있는 커밋 메시지 사용 |

## 콘텐츠 운영 규칙

- 초안은 항상 `docs/preview/`에 작성한다.
- 발행본은 검토 후 `content/posts/YYYY/`로 이동한다.
- 슬러그는 파일명에서 확장자를 제거한 값이다.
- 새 글을 발행할 때는 관련 이미지가 있으면 `public/images/` 하위에 둔다.
- `credentials.json`, `.env.local` 등 비밀값은 커밋하지 않는다.

## 테스트와 검증

- 기능 구현 후에는 관련 테스트를 반드시 실행한다.
- 기본 검증 명령은 `pnpm lint`와 `pnpm build`이다.
- 현재 lint 실패가 알려져 있더라도 새 작업 결과와 구분해 기록한다.
- 테스트가 실패하면 `feature-list.json`의 `passes`를 `true`로 바꾸지 않는다.

## 실패 대응

- 에러 메시지가 명확하면 해당 Phase 안에서 원인을 분석하고 수정한다.
- 같은 에러가 2회 이상 반복되면 수정 전에 원인과 계획을 정리한다.
- 같은 에러가 3회 이상 반복되면 사용자에게 현재 상태를 보고한다.
- 전체 작업을 되돌리지 말고, 문제가 생긴 Phase만 되돌린다.
